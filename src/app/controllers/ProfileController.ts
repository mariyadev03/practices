import Controller from 'framework/base/Controller';

export default class ProfileController extends Controller {
  public get namespace (): string {
    return 'app/controllers/ProfileController';
  }
}
